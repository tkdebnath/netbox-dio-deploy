---
phase: 01-core-model
plan: 01
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/netbox_dio/models/device.py
  - src/netbox_dio/models/__init__.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-04
  - CORE-05
  - DEV-01
  - DEV-02
  - DEV-03
  - DEV-04
  - DEV-05
  - DEV-06
  - DEV-07
  - DEV-08
user_setup: []
must_haves:
  truths:
    - "User can create a DiodeDevice instance with mandatory fields (name, site, device_type, role)"
    - "User cannot create a DiodeDevice without required fields - validation raises error"
    - "User can parse a nested dictionary into a DiodeDevice using from_dict() method"
    - "User can set optional device attributes (serial, asset_tag, platform, status, custom_fields, business_unit)"
    - "Pydantic validation enforces type correctness at initialization time"
    - "Status field is validated against allowed values (active, offline, planned)"
    - "DiodeDevice can convert to netboxlabs.diode.sdk.ingester.Device protobuf"
  artifacts:
    - path: "src/netbox_dio/models/device.py"
      provides: "DiodeDevice class with Pydantic BaseModel"
      contains: "class DiodeDevice(BaseModel)"
      exports: ["name", "site", "device_type", "role", "serial", "asset_tag", "platform", "status", "custom_fields", "business_unit", "from_dict", "to_protobuf"]
    - path: "src/netbox_dio/models/__init__.py"
      provides: "Package exports for models"
      contains: "from .device import DiodeDevice"
  key_links:
    - from: "src/netbox_dio/models/device.py"
      to: "tests/models/test_device.py"
      via: "pytest fixtures"
      pattern: "def test_required_fields|def test_from_dict|def test_type_validation"
    - from: "src/netbox_dio/models/device.py"
      to: "netboxlabs.diode.sdk.ingester.Device"
      via: "to_protobuf method"
      pattern: "from netboxlabs.diode.sdk.ingester import Device"
---

## Plan 01-01: Core DiodeDevice Model

<objective>
Create the DiodeDevice Pydantic model with mandatory field validation and all device-level attributes.

Purpose: This is the foundational data model for the entire package. It must enforce required fields (name, site, device_type, role) and support all optional device attributes (serial, asset_tag, platform, status, custom_fields, business_unit).

Output: src/netbox_dio/models/device.py with DiodeDevice class
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-model/01-RESEARCH.md
@.planning/phases/01-core-model/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Create DiodeDevice model with required fields</name>
  <read_first>
    <file_path>/workspace/netbox-dio-deploy/netbox-dio-deploy/.planning/phases/01-core-model/01-RESEARCH.md</file_path>
  </read_first>
  <files>
    src/netbox_dio/models/device.py
  </files>
  <action>
Create the DiodeDevice Pydantic model with the following structure:

1. Required fields using `Field(...)`:
   - name: str
   - site: str
   - device_type: str
   - role: str

2. Optional fields using `Field(default=None)`:
   - serial: str | None
   - asset_tag: str | None
   - platform: str | None
   - status: str | None
   - custom_fields: dict[str, Any] | None
   - business_unit: str | None

3. Class Config with validate_assignment = True

4. Class method from_dict() using model_validate()

5. Method to_protobuf() to convert to netboxlabs.diode.sdk.ingester.Device

6. Status field validator that accepts only "active", "offline", "planned"
  </action>
  <verify>
    <automated>pytest tests/models/test_device.py -x --tb=short</automated>
  </verify>
  <done>
    - src/netbox_dio/models/device.py exists with DiodeDevice class
    - Device can be instantiated with required fields only
    - Device raises ValidationError for missing required fields
    - Device.from_dict() parses dictionary into model
    - Device.to_protobuf() returns Diode SDK Device object
  </done>
</task>

<task type="auto">
  <name>Export DiodeDevice from models package</name>
  <read_first>
    <file_path>/workspace/netbox-dio-deploy/netbox-dio-deploy/.planning/phases/01-core-model/01-RESEARCH.md</file_path>
  </read_first>
  <files>
    src/netbox_dio/models/__init__.py
  </files>
  <action>
Create src/netbox_dio/models/__init__.py that:
1. Imports DiodeDevice from device.py
2. Exports DiodeDevice at package level

This allows `from netbox_dio.models import DiodeDevice`.
  </action>
  <verify>
    <automated>python3 -c "from src.netbox_dio.models import DiodeDevice; print('Import successful')"</automated>
  </verify>
  <done>
    - src/netbox_dio/models/__init__.py exists
    - DiodeDevice importable from netbox_dio.models
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → DiodeDevice | Untrusted dictionary data must be validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | DiodeDevice fields | Mitigate | Pydantic Field(...) enforces required fields, type annotations prevent wrong types |
| T-01-01-02 | Spoofing | from_dict() parsing | Mitigate | model_validate() performs type coercion and validation |
| T-01-01-03 | Tampering | status field | Mitigate | @field_validator enforces allowed values {"active", "offline", "planned"} |

## Validation Architecture

| Property | Value |
|----------|-------|
| Framework | pytest |
| Quick run | pytest tests/models/test_device.py -x |
| Wave run | pytest tests/ |

## Test Coverage Map

| Req ID | Test Function | Status |
|--------|---------------|--------|
| CORE-01 | test_required_fields | Wave 0 |
| CORE-02 | test_from_dict | Wave 0 |
| CORE-04 | test_type_validation | Wave 0 |
| DEV-01 | test_optional_fields | Wave 0 |
| DEV-02 | test_device_type | Wave 0 |
| DEV-03 | test_role | Wave 0 |
| DEV-04 | test_platform | Wave 0 |
| DEV-05 | test_site | Wave 0 |
| DEV-06 | test_status | Wave 0 |
| DEV-07 | test_custom_fields | Wave 0 |
| DEV-08 | test_business_unit | Wave 0 |

## Python 3.10+ Configuration (CORE-05)

The model implementation must ensure Python 3.10+ compatibility:
- Use `Optional[str]` from typing module instead of `str | None` syntax
- Use `dict[str, Any]` type hints are supported in Python 3.10

The plan must include:
- A task in Plan 01-02 (Wave 1) that creates pyproject.toml with `requires-python = ">=3.10"`
- A task that creates .python-version file with "3.10"
</threat_model>

<verification>
- [ ] pytest tests/models/test_device.py passes for all 11 test cases
- [ ] DiodeDevice can be instantiated with required fields
- [ ] DiodeDevice.from_dict() parses nested dictionaries
- [ ] to_protobuf() produces valid Diode SDK Device object
- [ ] Invalid status raises ValueError
</verification>

<success_criteria>
- [ ] All 11 test cases pass
- [ ] Device model accepts required fields and rejects missing ones
- [ ] Optional fields work correctly
- [ ] Conversion to protobuf works
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-model/01-01-SUMMARY.md`
</output>
