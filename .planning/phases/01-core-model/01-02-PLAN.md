---
phase: 01-core-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/models/test_device.py
  - tests/conftest.py
  - tests/__init__.py
  - tests/models/__init__.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test package structure exists for pytest discovery"
    - "Conftest.py provides fixtures for DiodeDevice tests"
    - "11 test methods exist covering all requirements (CORE-01, CORE-02, CORE-04, DEV-01 through DEV-08)"
  artifacts:
    - path: "tests/__init__.py"
      provides: "Test package marker"
      contains: "#"
    - path: "tests/models/__init__.py"
      provides: "Models test subpackage marker"
      contains: "#"
    - path: "tests/conftest.py"
      provides: "Pytest fixtures for tests"
      exports: ["device_data", "required_only_data", "invalid_status_data", "missing_required_data"]
    - path: "tests/models/test_device.py"
      provides: "Unit tests for DiodeDevice"
      exports: ["test_required_fields", "test_from_dict", "test_type_validation", "test_optional_fields", "test_device_type", "test_role", "test_platform", "test_site", "test_status", "test_custom_fields", "test_business_unit"]
  key_links:
    - from: "tests/conftest.py"
      to: "tests/models/test_device.py"
      via: "pytest fixture usage"
      pattern: "def test_.*\\(.*device_data\\)"
    - from: "tests/models/test_device.py"
      to: "src/netbox_dio/models/device.py"
      via: "from netbox_dio.models import DiodeDevice"
      pattern: "from netbox_dio\\.models import DiodeDevice"
---

## Plan 01-02: Test Infrastructure (Wave 0)

<objective>
Create the test scaffolding that will verify the DiodeDevice model implementation.

Purpose: This is Wave 0 infrastructure. Tests must exist before implementation so the executor can run automated verification after building the model. This includes the test file with stubs for all 11 test cases, conftest.py for fixtures, and package initialization.

Output: tests/models/test_device.py, tests/conftest.py
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-model/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Create test package structure</name>
  <files>
    tests/__init__.py
    tests/models/__init__.py
  </files>
  <action>
Create test package directories and __init__.py files:
1. tests/__init__.py - empty init
2. tests/models/__init__.py - empty init

These files mark the directories as Python packages for pytest discovery.
  </action>
  <verify>
    <automated>ls tests/ tests/models/ && echo "Directories exist"</automated>
  </verify>
  <done>
    - tests/__init__.py exists
    - tests/models/__init__.py exists
    - pytest can discover tests in tests/ directory
  </done>
</task>

<task type="auto">
  <name>Create conftest.py with fixtures</name>
  <files>
    tests/conftest.py
  </files>
  <action>
Create tests/conftest.py with pytest fixtures:
1. device_data fixture - returns a dict with all required and optional fields
2. required_only_data fixture - returns dict with only required fields
3. invalid_status_data fixture - returns dict with invalid status value
4. missing_required_data fixture - returns dict missing required field

These fixtures will be used by all model tests to ensure consistent test data.
  </action>
  <verify>
    <automated>pytest tests/ --collect-only | grep "device_data"</automated>
  </verify>
  <done>
    - tests/conftest.py exists with 4 fixtures
    - fixtures visible to pytest discovery
  </done>
</task>

<task type="auto">
  <name>Create test_device.py with stubs</name>
  <files>
    tests/models/test_device.py
  </files>
  <action>
Create tests/models/test_device.py with 11 test functions:

1. test_required_fields() - verify missing required fields raise ValidationError
2. test_from_dict() - verify dictionary parsing works
3. test_type_validation() - verify Pydantic enforces types
4. test_optional_fields() - verify serial, asset_tag work
5. test_device_type() - verify device_type field
6. test_role() - verify role field
7. test_platform() - verify platform field
8. test_site() - verify site field
9. test_status() - verify valid status accepted
10. test_custom_fields() - verify custom_fields dict
11. test_business_unit() - verify business_unit field

All tests should be stubs that import DiodeDevice and run basic checks.
Use the fixtures from conftest.py.
  </action>
  <verify>
    <automated>pytest tests/models/test_device.py --collect-only</automated>
  </verify>
  <done>
    - tests/models/test_device.py exists with 11 test functions
    - All tests visible to pytest
    - pytest returns 11 collected items
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → DiodeDevice | Test data must properly exercise validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Test fixtures | Accept | No security risk - test-only data |

## Validation Architecture

| Property | Value |
|----------|-------|
| Framework | pytest |
| Quick run | pytest tests/ -x |
| Full suite | pytest tests/ |

## Wave 0 Completion Checklist

- [ ] tests/__init__.py created
- [ ] tests/models/__init__.py created
- [ ] tests/conftest.py with fixtures
- [ ] tests/models/test_device.py with 11 stub tests
- [ ] pytest.ini or pyproject.toml exists for pytest config

## Python 3.10+ Configuration (CORE-05)

Add these tasks to configure Python version requirement:

<task type="auto">
  <name>Configure Python 3.10+ requirement</name>
  <files>
    pyproject.toml
    .python-version
  </files>
  <action>
Create Python version configuration:
1. pyproject.toml with [project] section containing requires-python = ">=3.10"
2. .python-version file with content "3.10" or "3.11"
  </action>
  <verify>
    <automated>cat pyproject.toml | grep requires-python</automated>
  </verify>
  <done>
    - pyproject.toml has requires-python = ">=3.10"
    - .python-version exists with valid Python 3.10+ version
  </done>
</task>
</threat_model>

<verification>
- [ ] pytest --collect-only shows 11 tests in tests/models/test_device.py
- [ ] conftest.py fixtures are discoverable
- [ ] Test package structure valid for pytest
</verification>

<success_criteria>
- [ ] pytest can discover all 11 test methods
- [ ] Test files commit to git
- [ ] Test infrastructure ready for model implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-model/01-02-SUMMARY.md`
</output>
